# Randall.VulnAi

Fictional **RAG1** AI-gateway lab — intentional length / auth-skip / tool-bridge crashes that mirror common LLM-codegen mistakes.

**Not** an LLM, OpenAI/Anthropic API, agent runtime, or tool executor. Loopback by default. No model calls, no shell.

Annotated `BEGIN AI` blocks so Bug Hunter can attribute the weak handlers.

See [docs/AI_LAB.md](../../docs/AI_LAB.md).
