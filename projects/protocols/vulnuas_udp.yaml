name: vulnuas-udp
description: UAS1 UDP telemetry — magic + msgId + BE length + payload
blocks:
  - type: static
    value: "UAS1"
  - type: bytes
    name: msg_id
    mutable: true
    minSize: 1
    maxSize: 1
    seedFile: seeds/vulnuas_udp_msgid_nav.bin
  - type: sized
    lengthName: telem_len
    lengthBytes: 2
    littleEndian: false
    child:
      type: bytes
      name: payload
      mutable: true
      minSize: 4
      maxSize: 512
      seedFile: seeds/vulnuas_udp_payload.bin
