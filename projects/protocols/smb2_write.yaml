name: smb2-write
description: NBSS + SMB2 Write — data after header is crashable on VulnSmb
blocks:
  - type: bytes
    name: pdu
    mutable: true
    minSize: 68
    maxSize: 8192
    seedFile: seeds/smb2_write.bin
